[project]
name = "rim-python-listener"
version = "0.1.0"
description = "Python listener for RoboticsIpcModule RouterFrame packets"
requires-python = ">=3.10"
dependencies = [
  "rich>=13.7.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
